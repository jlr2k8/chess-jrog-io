{
  "name": "chess-net",
  "private": true,
  "version": "0.1.0",
  "description": "chess.jrog.io — play chess against the computer",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "dns:upsert": "node scripts/upsert-dns.mjs"
  },
  "devDependencies": {
    "@aws-sdk/client-route-53": "^3.948.0",
    "concurrently": "^9.1.2"
  }
}
